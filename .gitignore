# Excluir la carpeta dist de ser ignorada (al incluirlo directamente añade de nuevo los archivos (de letra sombreada a normal))
!client/dist/
!client/dist/**

# No ignorar la carpeta assets y su contenido
!client/dist/
!client/dist/**
!client/dist/assets/
!client/dist/assets/**

# El archivo .vercel es un directorio creado por la CLI de Vercel (Vercel CLI) que contiene la configuración y la información necesaria para que Vercel pueda gestionar y desplegar tu proyecto. 
# Esto asegura que Vercel gestione correctamente el despliegue
# No ignorar el directorio .vercel dentro de client y todo su contenido
!client/.vercel/
!client/.vercel/**
