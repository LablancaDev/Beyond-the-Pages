# Node modules
node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov/

# Coverage directory used by testing tools like istanbul
coverage/

# nyc test coverage
.nyc_output/

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt/

# Bower dependency directory (https://bower.io/)
bower_components/

# Compiled source files
# Ignorar la carpeta build de manera general
# build/

# Ignorar el output del servidor si se genera en /server/dist
# Permitir la carpeta dist del cliente y el servidor
# Pero ignorar el contenido específico no necesario dentro de dist
dist/*
!dist/assets/ # Permitir subir la carpeta assets
!dist/index.html # Permitir subir index.html

# Ignorar la carpeta del servidor dist
server/dist/
!server/dist/** # Permitir que se suba el contenido del servidor dist

# Ignorar archivos de entorno
.env
.env.*   # Para archivos .env específicos de cada entorno

# IDE specific
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Yarn Integrity file
.yarn-integrity

# Parcel-bundler cache (if using Parcel)
.cache/

# Next.js build output (if you're using Next.js)
.next/

# Vercel deployments (asegúrate de incluir vercel.json)
# .vercel/ se excluirá si es necesario para la configuración local, pero mantén vercel.json
.vercel/*
!vercel.json    # <-- Asegúrate de NO ignorar el archivo de configuración de Vercel

# Ignore server build output if it goes to /server/dist (ajusta la ruta si es necesario)
# No necesitamos ignorar las carpetas dist aquí ya que son necesarias para el despliegue
# server/dist/



