{
  "version": 2,
  "builds": [
    {
      "src": "server/src/**/*.js",
      "use": "@vercel/node",
      "config": {
        "distDir": "server/dist"
      }
    },
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/dist/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/client/dist/$1"
    }
  ]
}


  